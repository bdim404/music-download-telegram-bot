# Telegram Bot Token
bot_token: "YOUR_BOT_TOKEN"

# Apple Music cookies file path
cookies_path: "./cookies.txt"

# Super administrator Telegram user IDs.
# Admins can use /allow and /deny, receive startup/shutdown/error/health notifications,
# and are always allowed to use the bot.
admin_users:
  - 1234567890

# User IDs allowed to use the bot
whitelist_users:
  - 987654321

# Group IDs allowed to use the bot
whitelist_groups:
  - -1234567890123

# Maximum concurrent downloads per user
max_concurrent_per_user: 2

# Maximum concurrent downloads globally
max_concurrent_global: 5

# Maximum file size in MB
max_file_size_mb: 50

# Database file path for caching
database_path: "./data/cache.db"

# Temporary directory for downloads
temp_path: "./data/temp"

# Default audio codec for downloads.
# Users can override this with /codec; user preference has the highest priority.
# Available options:
#   - "atmos"         : Dolby Atmos (eac3, 768kbps, multi-channel) - requires wrapper
#   - "alac"          : Apple Lossless (1600kbps+, lossless) - requires wrapper
#   - "aac"           : AAC 256kbps (standard quality)
#   - "aac-legacy"    : AAC Legacy codec
#   - "aac-he"        : AAC High Efficiency
#   - "aac-binaural"  : AAC Binaural
#   - "ac3"           : AC3 codec
# 
# Note: If a song doesn't support atmos/alac, it will automatically fallback to aac
song_codec: "atmos"

# Enable wrapper service availability for ALAC/Dolby Atmos downloads.
# This does not force every download to use wrapper-only codecs; it only allows
# users or the global default codec to select them.
# Wrapper service: https://github.com/WorldObservationLog/wrapper
use_wrapper: true

# Wrapper service address (host:port)
wrapper_url: "127.0.0.1:10020"

# Bot API health check interval in seconds.
# systemd watchdog is controlled by WatchdogSec in music-download-bot.service.
health_check_interval_seconds: 300
